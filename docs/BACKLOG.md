# BACKLOG

状态机：`idea → roadmap → spec'd → shipped(R#)`。

## 必做

| id | 方向 | 优先级 | 状态 | 关联 |
|----|------|--------|------|------|
| R1 | 统一索引并检索 Claude Code 与 Codex 的全部会话记录：Go 常驻单例 + systemd --user + MCP 端点 + 只读 Web dashboard（形态照搬同作者的 specloop）。解决「找不到上次那段对话」——决策与结论大量沉淀在会话里却无检索入口。 | P1 | shipped(R1) |  |
| R2 | dashboard 视觉与交互重做：走 claude-design 出设计稿（明暗双主题 design tokens + 五页组件规范）再实现，并补齐三项缺失能力——明暗切换、摘要浏览入口、设置页（可视化改 config 并生效）。设置页需修订「Web dashboard 为只读」这条 Usability 需求（只读的本意是不碰会话原始文件，该安全不变式不动）。 | P1 | shipped(R2) | 用户反馈：UI 丑、只有暗色无切换、不知道去哪看摘要、没有设置界面；验收走 design-handoff-parity |
| R3 | 前端路由与内容渲染：视图/检索词/过滤条件/正在读的会话全部进 URL（可分享、后退键可用）；assistant 正文按 Markdown 渲染并强制过消毒；工具输出的 ANSI 正确上色。 | P1 | shipped(R3) | 用户反馈：路由不变、exec 要终端渲染、文字要 Markdown |
| R4 | 工具调用按结构渲染：一张映射表 + 三种骨架（命令 / 文件 / patch），命令可直接复制执行、Edit 前后对照、apply_patch 带色 diff。 | P1 | shipped(R4) | tool_use 占内容块 39.4% |
| R5 | 会话名：解析 custom-title / agent-name 并回填历史，三个列表优先显示人写的名字而非机器摘要。 | P2 | shipped(R5) | 覆盖 2%，但正是最想再找到的那些 |
| R6 | 代码轻量着色：零依赖三态机，只认注释/字符串/数字。**已被 R7 推翻并删除**。 | P2 | shipped(R6) | 见 architecture 决策 14 |
| R7 | 语法高亮与图表：vendored highlight.js（配色可在设置页选，默认跟随界面主题）+ mermaid 按需渲染（3.4 MB 不进首屏）。推翻 R3/R4/R6 三次「代码块只占 2–5%」的判断——那个统计漏了 tool_use 的命令（52.1%）。 | P1 | shipped(R7) | 用户反馈：exec 没有语法高亮；决策 15/16 |
| R8 | 子代理串起来：主会话/子代理三态过滤 + 回读页父子互跳。库里 48.5% 的会话是子代理，此前界面上完全看不出来。 | P1 | shipped(R8) | 用户反馈：筛选里没法筛主会话/子agent；决策 17 |
| R9 | 静默失效清扫：四类「失败没有信号」的缺陷（吞错误让守卫失效 / 同步读异步配置 / 断言取样比命题宽 / 消毒器静默改内容）逐类查存量并修，另建可复跑的变异扫描工具。 | P1 | shipped(R9) | R7/R8 验收撞出的 5 个 bug 归纳而来；决策 18/19 |
| R10 | 工具结果里的源码也上语法高亮：语言只从命令判定（逐段），判不出一律不涂。tool_result 占内容块 38.9%，其中读文件类命令占 56.1%。 | P1 | shipped(R10) | 用户反馈：exec 里的代码还是没高亮；决策 20 |
| R11 | 摘要运维：修队列饥饿（failed 被无限复活饿死队列、failed 计数恒为 0）与空摘要的两个根因（Ollama num_ctx 默认 2048 静默截断、thinking 吃光 num_predict），新增生成时间窗口与「生成进度」页（含失败清单与重试）。 | P1 | shipped(R11) | 用户要求「设置里能设什么时候跑 + 要有页面展示进度」；排查中发现队列已卡死两天；决策 21 |
| R12 | 上下文预算对齐：分段参数改由 num_ctx 推导（此前 MaxInput 24000 字符 / ChunkSize 8000 字符 / num_ctx 8192 token 三个数从未对齐，分了段仍被截断），默认 num_ctx 提到 32768（实测只多 408 MiB），超长会话的中段不再整块丢弃（改均分分组 + 组内均摊配额）。 | P1 | shipped(R12) | 用户问「模型上下文不支持长文怎么解决 / 要不要换 12b」；决策 22/23 |
| R13 | 问答链路补上 num_ctx（R11 只修了摘要走的 /api/generate，问答走 /api/chat 一直吃服务端默认值，实测被截在 2051 token），options 装配抽成两条路径共用的一处。 | P1 | shipped(R13) | 用户问「问答用 4b 还是 12b / 设置要不要分开」时查出来的；决策 24/25 |
| R14 | 问一问支持指定项目范围（全部 / 某项目）。能力早在工具里（search_sessions 有 project 参数），只是前端只发 question、后端只收 question、筛选栏在该页隐藏。范围做成硬约束而非提示词，get_session 豁免，事件流发生效后的参数。 | P1 | shipped(R14) | 用户问「问一问怎么不能指定项目范围」；决策 26/27 |
| R15 | 对接 restic 做会话备份，chatdex 只做它做不到的那部分：可勾选的备份源、覆盖率校验（restic ls ⨯ sessions.file_path，回答「我索引过的会话备份里到底有没有」）、消失的会话直接从备份读回原件（索引对工具结果故意有损，706362 个块里 43637 个被截断，完整内容只在原件里）、备份页与可选依赖降级。明确不做定时调度 / 保留策略 / 代做 restore。 | P1 | shipped | 用户 /goal「对接 restic，但 chatdex 不做它的壳子——做它的大脑」；实测已有 183 个会话源文件消失、现有备份为零；决策 28 |

## Ideas

| id | 方向 | 优先级 | 状态 | 关联 |
|----|------|--------|------|------|
| I1 | CI：push / PR 上跑 `go test ./...` 与 `go vet`，多平台构建冒烟。 | P1 | shipped(2026-08-11) | 变异扫描是开发期工具，不进 CI |
| I2 | 发布 v0.1.0：打 tag + GitHub Release + 预编译二进制（linux/darwin × amd64/arm64）。 | P1 | shipped(2026-08-11) | README 已写从源码构建 |
| I3 | 推广：草稿在 `~/chatdex-promo-drafts.md`，未发布。发帖需本人操作最后一步。 | P3 | idea | |
| I4 | 活着的会话也能展开被截断的工具结果：索引把超 tool_result_cap（默认 4096）的工具结果截断，实测涉及 2567 个会话，其中 2416 个源文件**还在**。这类该直接读源文件那一段（有 file_path + offset），不该绕 restic——与 R15 的「从备份读原件」是两条不同的路径。界面上「已截断」那个角标现在只能看不能点。 | P2 | idea | R15 任务 4 量到的：43637/706362 个块被截断；reader.js 的 archivedBar 注释里记了这个分界 |
| I5 | 等第一个「被备份过之后才消失」的会话出现（覆盖率里 rescued_total &gt; 0），在真实语料上复看「从备份读回原件」。R15 任务 4 的验收标准是「挑一个真实的 alive=0 会话取回」，当时无法满足——183 个已消失的会话全都消失在第一次备份之前，而造一个就得删用户的真实文件。当时改用独立实例 + 合成会话走完了整条链路。这一步不是漏做，是当时做不了。 | P2 | idea | R15 任务 4；首次备份 2026-08-05，此前消失的 183 个救不回来 |
| I6 | 摘要长度约束形同虚设：提示词要求「不超过 60 字」，实测 1329/3155（42%）超出，靠 maxSummaryChars=120 的硬截断兜底。内容可读所以不算缺陷，但值得看看是提示词该改、还是这条要求本身不现实（60 字装不下「哪个项目 + 做了什么 + 结论」三件事）——若是后者，把要求改成 100 字更诚实，也省得模型每次都在违反它。 | P3 | idea | 2026-08-05 修提示词泄漏时量到；决策 29 |
| I7 | 认领 LobeHub 的 MCP 收录页（lobehub.com/mcp/cygmris-chatdex）。它自动抓的是 2026-07-31 的 R6 时代 README（特性表没有子代理/备份/进度页，检索延迟还写 23ms），且自动生成的 MCP 配置是 stdio 形式（command/args）而 chatdex 只有 Streamable HTTP，照抄的人会卡死。页面上的 Refresh Metadata 点了报「Workflow trigger is not configured」——未认领的收录用不了，需要先注册 LobeHub 账号认领。README 已加「不是 stdio」的明确警告，下次它自动重抓时应能改善。 | P3 | idea | 2026-08-11 发现；需本人注册第三方账号，不代办 |
